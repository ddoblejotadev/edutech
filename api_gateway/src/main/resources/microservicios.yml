# Configuración de URLs de microservicios
microservicios:
  persona:
    url: http://localhost:8081
    endpoints:
      base: /api/personas
      byRut: /api/personas/{rut}
      authenticate: /api/personas/auth
  
  curso:
    url: http://localhost:8082
    endpoints:
      base: /api/cursos
      byId: /api/cursos/{id}
      featured: /api/cursos/destacados
  
  ejecucion:
    url: http://localhost:8084
    endpoints:
      base: /api/ejecuciones
      byId: /api/ejecuciones/{id}
      inscripciones: /api/inscripciones
      inscripcionesByPersona: /api/inscripciones/persona/{rutPersona}
